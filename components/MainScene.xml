<?xml version="1.0" encoding="utf-8" ?>
<component name="MainScene" extends="Scene">
  <script type="text/brightscript" uri="pkg:/components/MainScene.brs"/>
  <script type="text/brightscript" uri="pkg:/components/EPGData.brs"/>
  <!-- Shared enums -->
  <script type="text/brightscript" uri="pkg:/source/enums/ColorPalette.brs" />
  <script type="text/brightscript" uri="pkg:/source/enums/URLs.brs" />
  <script type="text/brightscript" uri="pkg:/source/enums/TeamCodes.brs" />
  <script type="text/brightscript" uri="pkg:/source/enums/Utils.brs" />
  <script type="text/brightscript" uri="pkg:/source/enums/BitmapCache.brs" />

  <children>
    <!-- Video Player (bottom layer) -->
    <Video
        id="videoPlayer"
        visible="false"
        translation="[0,0]"
        width="1920"
        height="1080"
        opacity="1.0"/>

    <!-- Black loading overlay to hide video changes -->
    <Rectangle id="blackFlashOverlay"
        width="1920" height="1080"
        color="0x00000000"
        visible="false" />

    <!-- Video Info Overlay -->
    <VideoInfoOverlay
        id="videoInfoOverlay"
        translation="[0,0]"/>

    <!-- Multiview Grid -->
    <MultiviewGrid
        id="multiviewGrid"
        visible="false"
        translation="[0,0]"/>

    <!-- Black overlay for dimming video -->
    <Rectangle
        id="videoOverlay"
        visible="false"
        translation="[0,0]"
        width="1920"
        height="1080"
        color="0x000000"
        opacity="0.9"/>

    <!-- Main Guide Container -->
    <Rectangle
        id="guideBackground"
        visible="false"
        translation="[0,0]"
        width="1920"
        height="1080"
        color="0x0F0F0FFF"/>

    <!-- Header Section -->
    <Rectangle
        id="headerBackground"
        visible="false"
        translation="[0,0]"
        width="1920"
        height="200"
        color="0x1A1A1AFF"/>

    <!-- Featured Program Info -->
    <Poster
        id="featuredLogo"
        visible="false"
        translation="[60,50]"
        loadWidth="200"
        loadHeight="100"
        width="200"
        height="100"
        loadDisplayMode="scaleToFit"/>

    <Label
        id="featuredTitle"
        visible="false"
        translation="[340,55]"
        width="800"
        height="50"
        font="font:LargeBoldSystemFont"
        color="0xFFFFFFFF"/>

    <Label
        id="featuredTime"
        visible="false"
        translation="[340,115]"
        width="400"
        height="35"
        font="font:SmallBoldSystemFont"
        color="0xCCCCCCFF"/>

    <Label
        id="featuredDescription"
        visible="false"
        translation="[340,155]"
        width="1000"
        height="30"
        font="font:SmallBoldSystemFont"
        color="0x888888FF"/>

    <!-- Current Time Display -->
    <Label
        id="currentTimeLabel"
        visible="false"
        translation="[1550,60]"
        width="300"
        height="50"
        horizAlign="right"
        font="font:LargeBoldSystemFont"
        color="0xFFFFFFFF"/>

    <!-- Guide Header -->
    <Label
        id="guideHeaderLabel"
        visible="false"
        translation="[60,220]"
        width="150"
        height="40"
        text="Guide"
        vertAlign="center"
        font="font:MediumBoldSystemFont"
        color="0xFFFFFFFF"/>

    <Label
        id="allChannelsLabel"
        visible="false"
        translation="[60,265]"
        width="200"
        height="30"
        text="All Channels"
        vertAlign="center"
        font="font:SmallSystemFont"
        color="0x777777FF"/>

    <!-- Time Slot Headers Container -->
    <Group
        id="timeSlotHeadersContainer"
        visible="false"
        translation="[400,230]">

        <!-- Time Slot Headers -->
        <LayoutGroup
            id="timeSlotHeaders"
            translation="[2,0]"
            layoutDirection="horiz"
            itemSpacings="[0]"/>
    </Group>

    <!-- Channel Grid -->
    <MarkupList
        id="channelList"
        visible="false"
        translation="[60,310]"
        itemComponentName="GuideChannelItem"
        itemSize="[1800,80]"
        itemSpacing="[0,2]"
        drawFocusFeedback="false"/>

    <!-- Loading / Error Label -->
    <Label
        id="loadingLabel"
        text="Loading TV Guide..."
        translation="[540,500]"
        width="840"
        height="60"
        horizAlign="center"
        vertAlign="center"
        font="font:LargeBoldSystemFont"
        color="0xFFFFFFFF"/>

    <!-- Channel Menu (right side) -->
    <SlideChannelMenu
        id="channelMenu"
        visible="false"
        translation="[0,0]"/>

    <!-- Preload Container for BitmapCache -->
    <Group
        id="preloadContainer"
        visible="false"
        translation="[0,0]"/>
  </children>
</component>